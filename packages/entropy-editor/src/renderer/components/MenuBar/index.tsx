import type { ReactElement } from 'react';

export function MenuBar(): ReactElement {
  // Native menu is handled by app-menu.ts in the main process.
  // This component is a placeholder for additional in-renderer toolbar items.
  return <></>;
}
