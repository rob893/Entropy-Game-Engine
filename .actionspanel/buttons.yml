hideHelp: true
buttons:
  - title: Run tests
    action: "run_tests"
    description: Run your tests

  - title: Publish and Deploy
    action: "manual_publish_and_deploy"
    description: Publish Packages and Deploy. Confirm format {confirmation}:{(optional)branch}
    arguments:
      - name: Confirm

  - title: Deploy
    action: "manual_deploy"
    description: Deploy to remote server. Confirm format {confirmation}:{(optional)branch}
    arguments:
      - name: Confirm

  - title: Publish
    action: "manual_publish"
    description: Publish Packages. Confirm format {confirmation}:{(optional)branch}
    arguments:
      - name: Confirm
